package com.crcafe.core.model;

/**
 * Enum to represent the roles a user can have.
 * This ensures that only valid roles can be assigned.
 */
public enum UserRole {
    OWNER,
    MANAGER,
    WORKER
}